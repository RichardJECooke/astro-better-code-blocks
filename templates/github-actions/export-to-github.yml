# Exports extractedcode project directories to external GitHub repositories on merge to main.
# Useful when your code examples need to live in separate repositories (e.g. for GitHub import links
# in docs, or so readers can clone them directly).
#
# This template requires substantial customization -- see REPLACE comments throughout.
#
# Prerequisites:
#   - A GitHub PAT with write access to the target repositories, stored as a repository secret.
#   - An export script (see src/scripts/export-extractedcode.sh in astro-better-code-snippet-extractor
#     for a reference implementation) that handles the actual mirror logic.
#
# REPLACE: 'SNIPPET_PUBLISH_TOKEN' with your actual secret name.
# REPLACE: the paths trigger if your extractedcode directory has a different path.
# REPLACE: the export script path and arguments to match your implementation.
# REPLACE: working-directory / path references if your Astro project is not in astro/.

name: Publish extractedcode to external repositories

on:
  push:
    branches:
      - main
    paths:
      - 'astro/extractedcode/**'
  workflow_dispatch:

jobs:
  discover:
    name: Discover changed extractedcode directories
    runs-on: ubuntu-latest
    outputs:
      filter: ${{ steps.scan.outputs.filter }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - id: scan
        name: Find changed extractedcode directories
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # Manual trigger: publish all directories
            echo "filter=" >> "$GITHUB_OUTPUT"
            echo "Publishing all extractedcode directories"
          else
            # Push to main: only publish directories that changed
            dirs=()
            for dir in astro/extractedcode/*/; do
              name=$(basename "$dir")
              if ! git diff --quiet HEAD~1..HEAD -- "astro/extractedcode/$name/"; then
                dirs+=("$name")
              fi
            done
            if [ "${#dirs[@]}" -eq 0 ]; then
              echo "No extractedcode directories changed"
              echo "filter=__none__" >> "$GITHUB_OUTPUT"
            else
              echo "filter=${dirs[*]}" >> "$GITHUB_OUTPUT"
              echo "Publishing: ${dirs[*]}"
            fi
          fi

  publish:
    name: Publish extractedcode to external repositories
    needs: discover
    if: ${{ needs.discover.outputs.filter != '__none__' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: astro/package-lock.json

      - name: Install dependencies
        working-directory: astro
        run: npm ci

      # REPLACE: update the script path and argument order to match your export script.
      # The reference implementation takes a GitHub PAT and commit SHA as positional arguments,
      # and reads the EXTRACTEDCODE_FILTER env var to decide which directories to publish.
      - name: Publish extractedcode to external repositories
        env:
          EXTRACTEDCODE_FILTER: ${{ needs.discover.outputs.filter }}
        run: bash src/scripts/export-extractedcode.sh ${{ secrets.SNIPPET_PUBLISH_TOKEN }} ${{ github.sha }}
        working-directory: astro
